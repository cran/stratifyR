% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.strata.R
\name{plot.strata}
\alias{plot.strata}
\title{Plot Method for Stratified Survey Design Objects}
\usage{
\method{plot}{strata}(
  x,
  type = c("2d", "3d", "interactive"),
  data = NULL,
  n_pts = 512L,
  alpha = 0.25,
  palette = c("#4E79A7", "#F28E2B", "#E15759", "#76B7B2", "#59A14F",
    "#EDC948", "#B07AA1", "#FF9DA7", "#9C755F"),
  main = NULL,
  show_info = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{"strata"} object.}

\item{type}{\code{"2d"} (default), \code{"3d"}, or \code{"interactive"}.}

\item{data}{Optional numeric vector of population values.}

\item{n_pts}{Integer. Density grid size. Default \code{512L}.}

\item{alpha}{Numeric. Stratum fill transparency. Default \code{0.25}.}

\item{palette}{Character vector of stratum colours (recycled).}

\item{main}{Character. Plot title (auto-generated if \code{NULL}).}

\item{show_info}{Logical. Overlay a per-stratum information box on the
\code{"2d"} plot. Default \code{TRUE}.}

\item{...}{Passed to \code{hist()} or \code{plotly::layout()}.}
}
\value{
Invisibly returns \code{x} for \code{"2d"}; a \pkg{plotly} widget
for \code{"3d"} and \code{"interactive"}.
}
\description{
Visualises a stratified design. The \code{"2d"} mode draws a histogram with
the fitted density overlaid and the strata shaded; \code{"3d"} and
\code{"interactive"} render \pkg{plotly} views of the Neyman cost surface.
}
\examples{
\dontrun{
set.seed(1); y <- rgamma(2000, shape = 2, rate = 0.5)
res <- strata.data(y, h = 4, n = 400)
plot(res)
plot(res, type = "3d")
plot(res, type = "interactive")
}
}
